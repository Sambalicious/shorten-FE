export interface IData {
  shortUrl?: string;
  longUrl?: string;
  shortCode?: string;
  createdAt?: string;
  updatedAt?: string;
}

export interface IResponseError {
  message: string;
}

export interface longUrlData {
  longUrl: string;
}
